  * fling rotation, look at nehe-android for info
  * TestRenderer -> MeshRenderer
  * OFFReader -> OffReader
    * BrowseElement.validExensions() is a HashSet that contains all extensions we should consider
  * abstract class Reader, OffReader extends Reader
  * BrowseElement has abstract method getReader() that returns either OffReader or ObjReader
    * getReader() uses Reader.newInstance(path) ... static factory method that maps file extension to Reader impl
    * some BrowseElement.getReader() impls would return UnsupportedOperationException 
    * GL activity calls BrowseElement.getReader(), then Reader.getMesh(), and constructs MeshRenderer with the returned Mesh object 
  * ObjReader 
    * http://en.wikipedia.org/wiki/Obj
    * samples,
      * http://groups.csail.mit.edu/graphics/classes/6.837/F03/models/
      * http://people.sc.fsu.edu/~jburkardt/data/obj/obj.html
  * fog, also look at nehe-android
  * preferences
    * draw back faces toggle
    * fog on / off toggle
  * light rotation, perhaps toggle between object and this